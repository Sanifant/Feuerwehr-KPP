name: CI - Build and Test

on:
  push:
    branches: ['**']
  pull_request:
    branches: [main, master, develop]

env:
  DOTNET_VERSION: '10.0.x'
  NODE_VERSION: '22.12.0'

jobs:
  backend-linux:
    name: Backend (Linux)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: |
          dotnet restore src/Backend/Feuerwehr.Server/Feuerwehr.Server.csproj

      - name: Build
        run: |
          dotnet build src/Backend/Feuerwehr.Server/Feuerwehr.Server.csproj -c Release --no-restore

      - name: Test
        shell: pwsh
        run: |
          $testProjects = Get-ChildItem -Path src -Recurse -Filter *.csproj | Where-Object { $_.Name -match 'test' }
          if (-not $testProjects) {
            Write-Host "No test projects found. Skipping tests."
            exit 0
          }

          foreach ($project in $testProjects) {
            Write-Host "Running tests for $($project.FullName)"
            dotnet test $project.FullName -c Release --verbosity normal --logger trx
          }

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-backend-linux
          path: '**/*.trx'

      - name: Upload code coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: code-coverage-backend-linux
          path: '**/coverage.cobertura.xml'

  frontend-web:
    name: Frontend Web (Linux)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: src/frontend/package-lock.json

      - name: Install dependencies
        working-directory: src/frontend
        run: npm ci

      - name: Build frontend
        working-directory: src/frontend
        run: npm run build

      - name: Upload frontend build artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: src/frontend/dist

  desktop:
    name: Desktop (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: |
          dotnet restore src/App/Feuerwehr.App/Feuerwehr.App.Desktop/Feuerwehr.App.Desktop.csproj

      - name: Build
        run: |
          dotnet build src/App/Feuerwehr.App/Feuerwehr.App.Desktop/Feuerwehr.App.Desktop.csproj -c Release --no-restore
#
#  android-linux:
#    name: Android (Linux)
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v4
#
#      - name: Setup .NET
#        uses: actions/setup-dotnet@v4
#        with:
#          dotnet-version: ${{ env.DOTNET_VERSION }}
#
#      # Install only what Android needs, and only in the Android job
#      - name: Install Android workload
#        run: |
#          dotnet workload install android --skip-manifest-update \
#            --source https://api.nuget.org/v3/index.json \
#            --source https://aka.ms/dotnet9/nuget/index.json
#
#      - name: Restore Android
#        run: |
#          dotnet restore src/Mobile/mobile.Android/de.openelp.feuerwehr.mobile.Android.csproj
#
#      - name: Build Android
#        run: |
#          dotnet build src/Mobile/mobile.Android/de.openelp.feuerwehr.mobile.Android.csproj -c Release --no-restore
#
#      - name: Test Android
#        run: |
#          dotnet test src/Tests/Mobile/mobile.Android.UnitTests/de.openelp.feuerwehr.mobile.Android.UnitTests.csproj -c Release --verbosity normal --logger trx
#
#      - name: Upload test results
#        if: always()
#        uses: actions/upload-artifact@v4
#        with:
#          name: test-results-android-linux
#          path: '**/*.trx'
#
#  ios-macos:
#    name: iOS (macOS)
#    runs-on: macos-latest
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v4
#
#      - name: Setup .NET
#        uses: actions/setup-dotnet@v4
#        with:
#          dotnet-version: ${{ env.DOTNET_VERSION }}
#
#      - name: Install iOS workload
#        run: |
#          dotnet workload install ios --version 10.0.200 \
#            --source https://api.nuget.org/v3/index.json \
#            --source https://aka.ms/dotnet9/nuget/index.json
#
#      - name: Restore iOS
#        run: |
#          dotnet restore src/Mobile/mobile.iOS/de.openelp.feuerwehr.mobile.iOS.csproj
#
#      - name: Build iOS
#        run: |
#          dotnet build src/Mobile/mobile.iOS/de.openelp.feuerwehr.mobile.iOS.csproj -c Release --no-restore